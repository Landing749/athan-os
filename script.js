// Memory core boot sequence
// They told me not to run this function again. I did anyway.

console.log("ATHAN.OS: Initialization complete.");

let boot = true;
if (boot) {
  console.log("You found the log. That means you're one of us.");
}
